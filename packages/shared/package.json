{
  "name": "@voeq/shared",
  "version": "0.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@voeq/eslint-config": "workspace:*",
    "@voeq/tsconfig": "workspace:*",
    "eslint": "^8.57.0",
    "typescript": "^5.5.4"
  }
}
